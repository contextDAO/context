import { DappContext } from "../types/types";
/**
 * mintTokens
 *
 * @param {DappContext} dapp
 * @param {number} qty
 */
export default function mintTokens(dapp: DappContext, qty: number): Promise<void>;
